import styled from 'styled-components';

const Background = styled.div``;
const Container = styled.header``;
const Frame = styled.div``;

const Logo = styled.img``;

export { Container, Background, Frame, Logo };
